#!/usr/bin/env bash
# Display all active IPv4 IPs on the machine

# Use the 'ip' command to retrieve the IPs
ips=$(ip -4 addr show | grep inet | awk '{print $2}' | cut -d '/' -f1)

# Display each IP address on a separate line
echo "$ips"
